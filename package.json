{
  "name": "ultimate-bookkeeping-v2",
  "private": true,
  "description": "Ultimate Bookkeeping v2 monorepo (JS/TS side). apps/api is a separate Python project, not part of this workspace.",
  "workspaces": [
    "apps/outlet",
    "packages/*"
  ],
  "scripts": {
    "dev:outlet": "npm run dev --workspace=apps/outlet",
    "build:outlet": "npm run build --workspace=apps/outlet",
    "test:offline-queue": "npm run test --workspace=packages/offline-queue"
  }
}
